package phases;

@FunctionalInterface
public interface Phase2i {
	
	/**
	 * Creates a Functional Interface containing 
	 * an abstract method, which returns a double value.
	 * @return double
	 */
	abstract double getPiValue();
	
}
